"use client";

import ClassList from "@/components/ClassList";

/**
 * Klasselista til utskrift — egen adresse, som samtalene, og ikke et vindu:
 * arket er en hel side bredt, og det er noe læreren skriver ut og går fra.
 */
export default function ClassListPage() {
  return <ClassList />;
}
